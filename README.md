# EvalContext

A system for dependency injection and function evaluation.

## Developing and testing

With [uv](https://docs.astral.sh/uv/getting-started/installation/) installed, use `uv run pytest` to run the tests.
